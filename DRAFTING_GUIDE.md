# 초안 작성 프롬프트 (DRAFTING_GUIDE.md)

> `RAW_STORIES.md`의 원료를 실제 사이트 문구로 바꿀 때 따르는 규칙.
> 초안을 쓰기 전에 이 문서를 먼저 읽고, 여기 없는 판단이 필요하면 사용자에게 묻는다.
> 이 문서 자체도 원료가 쌓이며 패턴이 보이면 갱신한다.

---

## 1. 목적

한 사람의 날것 그대로의 이야기(대화체, 비문·반복 있음)를, **출처를 숨기지 않으면서도**
세 가지 다른 표면 — ① `docs/REVERSE_PLAN.md`(원본, 서술형 회고 문서) ② story 저장소의
`REVERSE_PLAN.md` 사본 + `reverse-plan.html`(정돈된 읽기용) ③ `index.html`(발표·공유용
랜딩페이지) — 에 맞는 문체로 옮긴다. **세 표면은 같은 사실을 다른 밀도로 말한다.**

## 2. 태그 → 목적지 → 형식 매핑

| 태그 | REVERSE_PLAN.md 위치 | index.html 위치 | 형식 |
|---|---|---|---|
| `HERO` | §1-0 중심 줄기 | 히어로 헤드라인/서브카피 | 인용구 또는 한 문장 선언. 최대 1~2개만 앞면에 노출 — 나머지는 §1 본문으로 |
| `PROBLEM` | §1-0·§1-1 | "원초적 고민" 섹션의 3영역 카드 | 구체적 증상 한 줄 + 근거(누가·언제 확인했는지) |
| `Q#` | §1-1 표의 해당 Q행 근거, §5 서비스 표 | 제품 투어 카드의 "왜" 태그 근거 | 기존 Q1~Q8 표에 이미 있으면 **새 Q를 만들지 말고 기존 행의 "구체적 증상"을 보강** |
| `DECISION` | §2 의사결정 문제 (신규 소절 또는 기존 소절에 서사 추가) | "만든 방식" 또는 별도 카드 | 갈림길(무엇을 고민했나) → 고른 것 → 왜. 표면적 질문과 실제로 멈춰 세운 불안을 구분(PO-3 패턴 따름) |
| `WEAK` | §4-5 알려진 약점 (기존 S#/D# 옆에 새 카테고리 필요하면 만듦, 억지로 S/D에 끼워넣지 않음) | "약점 공개" 섹션 | 원인→영향→설계 보완 3단 유지 |
| `TIMELINE` | §6 타임라인 | 타임라인 섹션 phase 칩 | 날짜 또는 주차 명시. 종료 조건이 날짜가 아니라 상태 기반이면 그대로 상태 기반으로 적는다(날짜로 억지 변환 금지) |
| `OTHER` | 재분류 후 위 규칙 적용 | 〃 | 재분류 전엔 게시하지 않는다 |

## 3. 문체 규칙 (표면별)

- **`REVERSE_PLAN.md`(원본·사본)**: 평서형 종결(`~다`), 3인칭 회고 톤, 기존 문서의 문단
  구조(리드 문장 → 근거 → 소스 링크)를 따른다. 새 절을 추가할 땐 기존 소절 번호 체계
  (§1-0, §1-1 …)를 침범하지 않고 다음 번호를 쓴다.
- **`reverse-plan.html`**: 위 원본을 그대로 옮기되 표·카드로 재조립. 새 산문을 짓지 않는다
  (원본에 없는 문장을 이 페이지에서 처음 만들지 않는다 — 원본 먼저, 사본은 그 반영).
- **`index.html`(쇼케이스)**: 2인칭·명령형·짧은 문장 허용. 그러나 **사실을 부풀리지 않는다**
  — "성장할 발판을 만들었다" 같은 자기 진술은 인용부호로 감싸고 "사용자 진술"임을
  표시하거나, 이미 코드·문서로 증명된 사실만 단정문으로 쓴다.

## 4. 원문 보존 규칙

- 원문을 그대로 쓰는 부분은 **인용부호 + 출처**를 남긴다 (`RAW_STORIES.md #번호` 를
  초안 옆 메모에 적어 검토자가 대조할 수 있게 한다. 게시 파일 자체에는 안 넣는다).
- 요약·각색한 부분은 초안 단계에서 **[각색]** 표시를 달아 사용자에게 보여준다.
  승인 후 최종 게시본에서는 표시를 뗀다.
- 은어·비문(예: "할려고", "이내용들") 은 다듬는다. 다만 **사용자 고유의 용어·조어
  ("기대정렬" 같은)는 절대 바꾸지 않는다** — 그 사람의 방법론이지 문장 오류가 아니다.

## 5. 사실 검증 규칙

- `DECISION`·`WEAK` 태그처럼 기술적 주장을 동반하는 원료는, 초안 전에 실제 코드·ADR과
  대조한다 (예: #3 "Notion은 대규모 파일 정리가 어려웠다" → 실제 `services/notion.py`가
  파일이 아니라 브리핑 텍스트만 다룬다는 사실과 모순 없는지 확인).
- 확인 안 된 주장은 "사용자 진술"로만 표시하고 사실 단정문으로 바꾸지 않는다.
- 이미 배포된 기능(예: OKR 구글 등급, ADR-0034)과 자연스럽게 연결되는 원료는 그
  기능의 ADR·FR 번호를 각주로 링크한다.

## 6. 승인 프로세스

1. 이 가이드에 따라 초안 작성 → 사용자에게 **원료 번호 + 태그 + 초안 + 각색 표시**
   함께 보여준다.
2. 사용자가 승인·수정 지시 → 반영.
3. 승인된 것만 실제 파일에 반영: `docs/REVERSE_PLAN.md`(원본, main 저장소) 먼저 →
   story 저장소 사본 → `reverse-plan.html` → `index.html` 순서. 원본 없이 사본만
   고치지 않는다.
4. `RAW_STORIES.md` 해당 항목 상태를 `반영됨`으로 바꾸고, 반영된 파일·위치를 메모에 남긴다.
5. story 저장소 커밋·푸시 → Pages 재배포 확인. main 저장소는 브랜치·PR 규칙(직접
   `main` 커밋 금지)을 그대로 지킨다.

## 7. 하지 않는 것

- 원료에 없는 에피소드를 지어내지 않는다.
- 한 원료를 여러 표면에 억지로 중복 배치하지 않는다 — 가장 잘 맞는 자리 하나를 고른다.
  (같은 내용을 다른 밀도로 두 표면에 걸쳐 쓰는 것은 허용 — §1 규칙 참고)
- 승인 전에 게시 파일을 고치지 않는다.
